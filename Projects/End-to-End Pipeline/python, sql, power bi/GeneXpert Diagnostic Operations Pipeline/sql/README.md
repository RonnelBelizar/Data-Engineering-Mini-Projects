## SQL Data Warehouse Scripts

This folder contains SQL scripts used to create and populate the MySQL data warehouse.

It includes scripts for staging table creation, dimension and fact table schema design, and dimension population from the staging layer. The warehouse follows a star schema optimized for analytical queries and Power BI reporting.

Referential integrity and fact table grain are enforced at the database level.